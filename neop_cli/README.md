# neop_cli — Core-bound protect PSBT and Electrum pairing hints (stdlib).

python -m neop_cli electrum-endpoints
python -m neop_cli protect-psbt --help

PYTHONPATH=. python3 -m unittest discover -s tests -v
