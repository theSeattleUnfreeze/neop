export default function TasksPlaceholderPage() {
  return (
    <section className="scoop-panel">
      <h2>Tasks</h2>
      <p className="muted">
        Split to-dos and replay-receive follow-ups land in the next Scoop PR. Open tasks already
        appear as counts on the dashboard after sync.
      </p>
    </section>
  );
}
